server:
  port: 8086

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: Arpitha
    password: Arpitha@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      pool-name: HikariMySQLPool
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: arpithahkarpithahk@gmail.com
    password: "ymtv pnim biyv qvkt"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.zaxxer.hikari: INFO
    org.springframework.security: DEBUG

jwt:
  secret: mysecretkeymysecretkeymysecretkey
  access-expiration: 600000        # 10 min
  refresh-expiration: 604800000    # 7 days

management:
  endpoints:
    web:
      exposure:
        include: "*"